function Certification(id, candidate, totalExperience, currentCompany, pastJobs, skills, currentSalary, expectedSalary, education, availableAfterDays) {
	this.id = id;
	this.name = name;
	this.issuingAuthority = issuingAuthority;
}

module.exports = Certification;